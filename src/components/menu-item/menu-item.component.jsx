/*
    deprecated, replaced by CategoryItem
*/

// import React from 'react';
// import './menu-item.styles.scss';

// const MenuItem = ({title, imageUrl, size}) => (
//     <div className={`menu-item ${size}`}>        {/* React gives every HTML element a property called "style" that takes an OBJECT that has prop values equal to CSS values that u wanna apply to the element. `url()` with `` means we do string interpolation w value of imageUrl  */}
//         <div className="background-image" style={{ backgroundImage: `url(${imageUrl})` }}>  {/* we put styles here cos when we hover n the image expands, we dont want the surrounding container to expand, we only want the pic to expand WITHIN the container */}

//         </div>
//         <div className="content">
//             <h1 className="title">{title.toUpperCase()}</h1>
//             <span className="subtitle">SHOP NOW</span>
//         </div>
//     </div>
// )

// export default MenuItem;
